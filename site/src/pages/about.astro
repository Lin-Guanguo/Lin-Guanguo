---
import BaseLayout from "../layouts/BaseLayout.astro";
import { getAboutPage } from "../lib/content.js";

const about = getAboutPage();
const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
---

<BaseLayout
  description="Public About Me profile for Lin Guanguo."
  title="About · Lin Guanguo"
>
  <main class="reader-shell">
    <nav class="reader-nav" aria-label="Breadcrumb">
      <a href={base}>Home</a>
      <span>/</span>
      <span>About</span>
    </nav>

    <article>
      <header class="article-header">
        <p class="eyebrow">About Me</p>
        <h1>{about.title}</h1>
        <p class="article-lead">AI Agent / Backend Systems Engineer</p>
        {
          about.lastUpdated && (
            <div class="meta-row">
              <span>Last updated {about.lastUpdated}</span>
            </div>
          )
        }
      </header>

      <div class="prose" set:html={about.html} />
    </article>
  </main>
</BaseLayout>
