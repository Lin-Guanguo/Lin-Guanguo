{
  "name": "lin-guanguo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:essays": "node scripts/sync-public-essays.mjs",
    "dev": "npm run sync:essays && astro dev --host 127.0.0.1",
    "build": "npm run sync:essays && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "test": "node --test src/lib/content.test.js src/config/deployment.test.js"
  },
  "dependencies": {
    "astro": "^6.3.3",
    "gray-matter": "^4.0.3",
    "marked": "^16.0.0"
  }
}
